import styled from 'styled-components';

import { BLACK } from '~src/constants/colors';

export const PageDesc = styled.section`
  margin: 15px 20px 15px 20px;

  font-size: 14px;
  font-weight: 600;
  color: ${BLACK};
`;
