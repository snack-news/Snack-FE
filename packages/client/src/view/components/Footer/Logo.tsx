import styled from 'styled-components';

import { LogoWhiteFooterImg } from '~src/resources';

export const Logo = styled.img.attrs({ src: LogoWhiteFooterImg })`
  width: 179px;
  margin: 0px auto;
`;
