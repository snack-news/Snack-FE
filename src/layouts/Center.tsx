/**
 * 가운데 정렬 레이아웃 컴포넌트
 */
import styled from 'styled-components';
import { BaseLayout } from './BaseLayout';

export const Center = styled(BaseLayout)`
  align-items: center;
  justify-content: center;
`;
