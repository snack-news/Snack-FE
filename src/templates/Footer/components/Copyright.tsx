import styled from 'styled-components';
import resources from '../../../resources';

const Copyright = styled.img.attrs({ src: resources.LogoWhiteFooterImg })`
  display: block;
  margin: 0 auto;
`;

export default Copyright;
